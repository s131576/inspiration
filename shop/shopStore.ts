import { create } from "zustand";
import {
Dimensions,Meta,Product,ProductsResponse,Review
} from "@/types";

export interface IStore {

  
}

const useStagairStore = create<IStore>((set) => ({
  

  
}));

export default useStagairStore;
